#include <stdio.h>
/**
 * main - Entry point
 *  Printf: with proper grammar, but the outcome is a piece of art,
 * Return: 0 (Success)
 */
int main(void)
{
printf("Size of a char %s", sizeof(char));
return (0);
}
